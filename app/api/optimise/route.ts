import {NextResponse} from "next/server";import {getBootstrap,getFixtures,getHistory,getPicks,getTeam} from "../../../lib/fpl";import {optimiseSquad} from "../../../lib/engine";
export async function GET(){const id=process.env.FPL_TEAM_ID||"1187241";try{const [b,t,h,f]=await Promise.all([getBootstrap(),getTeam(id),getHistory(id),getFixtures()]);const gw=Number(t.current_event||h.current?.at(-1)?.event||1);const picks=await getPicks(id,gw);const result=optimiseSquad(picks.picks||[],b.elements||[],f,gw,Number(picks.entry_history?.bank||0)/10,h);return NextResponse.json({connected:true,team:t,gw,picks,history:h,result,generatedAt:new Date().toISOString()})}catch(e){return NextResponse.json({connected:false,error:String(e)},{status:502})}}